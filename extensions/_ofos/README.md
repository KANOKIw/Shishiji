# ofos-kanokiw README

## setting
./.vscode/ofos.json
```json
{
    "startupfiles": [
        "app.ts",
        // open the all files in the directory
        "src/main/*",
        // exclude child directory
        "test/*^childdir",
        ...
    ]
}
```
